{% extends 'base.html.twig' %}

{% block title %}Hopital{% endblock %}

{% block body %}
    <h1>Hopital</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ hopital.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ hopital.nom }}</td>
            </tr>
            <tr>
                <th>Adresse</th>
                <td>{{ hopital.adresse }}</td>
            </tr>
            <tr>
                <th>Gps</th>
                <td>{{ hopital.gps }}</td>
            </tr>
            <tr>
                <th>Tarif</th>
                <td>{{ hopital.tarif }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('hopital_index') }}">back to list</a>

    <a href="{{ path('hopital_edit', {'id': hopital.id}) }}">edit</a>

    {{ include('hopital/_delete_form.html.twig') }}
{% endblock %}
